<!DOCTYPE html>
<html>
<head>
  <title>DealPlug</title>
  <%= stylesheet_link_tag "reset", "main", "scaffold" %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>

<div id="container">  
  <div id="banner">
    <%= link_to image_tag("logo.png"), root_path, :alt => "Latest deals" %>
    <h1 class="tagline">Find and share best online shopping deals</h1>
  </div>
  <div id="nav">
    <ul class="tabs">
      <li><%= link_to "Popular deals", root_path %></li>
      <!--li><%= link_to "Latest deals", latest_path %></li-->
    </ul>

    <% if user_signed_in? %>
      <%= link_to 'Submit a deal', new_deal_path, :id => "submit_deal" %>
      [<%= current_user.points %>] <%= current_user.username %> |
      <%= link_to "Sign out", destroy_user_session_path %>
    <% else %>
      <%= link_to "Sing up", new_user_registration_path %> or <%= link_to "sign in", new_user_session_path %>
    <% end %>
  </div>
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>
  <%= yield %>
</div>
</body>
</html>
